# Quét độc lập trước khi xem pre-label

Frame: ĐIỀN tên một ảnh trong `to_label/round1/images/train/` (frame_0369.jpg)

Số xe nhìn thấy bằng mắt: 38

Hai vị trí dễ bị AI bỏ sót hoặc vẽ sai, kèm mô tả xe: 3 xe ở chính giữa ảnh đè lên nhau(hướng từ phải sang trái, dưới lên trên), 1 đuôi xe góc phải dưới cùng 

Chạy `python3 tools/lock_blind.py` ngay sau khi điền. Sau đó giữ file này nguyên vẹn.
